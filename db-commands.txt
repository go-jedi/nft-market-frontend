CREATE TABLE bot_params(id INTEGER PRIMARY KEY NOT NULL, tele_id BIGINT NOT NULL, lang, watching_add_mam BOOLEAN DEFAULT(FALSE), watching_find_mam BOOLEAN DEFAULT(FALSE), watching_change_minlink BOOLEAN DEFAULT(FALSE), watching_add_balance BOOLEAN DEFAULT(FALSE), watching_change_minuser BOOLEAN DEFAULT(FALSE), watching_change_balance BOOLEAN DEFAULT(FALSE), watching_message_user BOOLEAN DEFAULT(FALSE), choose_tele_id BIGINT DEFAULT(0));
